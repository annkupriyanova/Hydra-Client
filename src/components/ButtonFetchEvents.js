import React from 'react';
import { Button } from 'react-bootstrap'

const ButtonFetchEvents = ({ onClick }) => (    
    <Button onClick={ onClick }>Get Events</Button>
)

export default ButtonFetchEvents;